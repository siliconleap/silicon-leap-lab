# YouTube 视频脚本

**实验：** 2026-08-markdown-only-content-pipeline
**预计时长：** 8-10 分钟（估算，未经剪辑验证）
**形式：** 录屏 + AI 配音 + 字幕，无出镜

> ## ⚠️ 制作状态：可制作，但本文件已过时
>
> 这份脚本写于「视频完全做不出来」的判断下。那个判断有两处错：
>
> | 环节 | 当时判断 | 实际 |
> | --- | --- | --- |
> | 脚本 | ✅ | ✅ 已由 `scenes.md` 取代（分镜式，可被脚本解析） |
> | 标题 / thumbnail 方案 | ✅ | ✅ `packaging.md` |
> | 数据卡 | ✅ | ✅ 可生成 |
> | **过程素材** | ❌ 0 个文件 | ⚠️ 录屏确实是 0，但会话记录在：`notes/session-timeline.md`，123 条真人输入，8/08–8/18 全程带时间戳 |
> | **剪辑** | ❌ 无自动化路径 | ✅ `scripts/build-video.py`，画面按配音时长铺开后 concat |
> | **配音** | ❌ 无自动化路径 | ✅ TTS（先用系统 `say` 跑通，可换 API） |
> | **字幕** | ❌ 无自动化路径 | ✅ 旁白即字幕，时间码由音频产生，不用对轴 |
>
> 当时的结论是「视频不是纯文本，也没法由纯文本生成」。这句话错在把**画面素材**
> 和**组装工序**混为一谈：组装是确定性的，纯文本描述加脚本就能做；真正缺的只是
> 一手画面素材，而那个缺口比想象的小——对话式工作的过程证据在会话记录里，
> 不在终端录屏里。
>
> **本文件保留作为当时判断的记录，不要照它制作。** 新分镜见 `scenes.md`。

---

## 素材清单

`recordings/` 下 **0 个文件**。但过程证据不止录屏一种——见 `notes/session-timeline.md`。

| 文件 | 时长 | 内容 | 用在 |
| --- | --- | --- | --- |
| — | — | 无录屏素材 | — |

可用的替代素材：`drafts/*/assets/` 下的数据卡与结构图（PNG，由 HTML 渲染），可作为无录屏时的画面填充。但纯图片撑不起 8 分钟。

## Hook（0:00 - 0:30）

**旁白：**

想做内容想了很久，一直没开工。工具在比较，方案在研究，最佳实践在找——就是没有一篇发出来。

后来我改了做法：把想试的东西都当成实验记下来。第一个实验，就是用 AI 把这套记录机制本身做出来。

半天搭完骨架。跑出第一篇之后，我发现它还不值得发布。

**画面：** 数据卡 —— 「半天做完 / 没法用」的反差卡（可复用 `drafts/xiaohongshu/assets/00-cover.png` 改横版）

## 背景（0:30 - 1:15）

**旁白：**

先说这套东西是什么。两个仓库，一个放流水线，一个放实验记录。全都是纯文本，没有服务器，没有数据库，也没用任何平台工具。这里的 AI agent 指协助写初稿和执行确定性检查的工作流，不替人确认事实或决定发布。

要验证的问题很简单：只靠 Markdown 约定加两个 skill，这条流水线能不能真跑起来。

答案是能。但这是这次实验里最不重要的一个结论。

**画面：** 结构图 —— 双仓 + 两个 skill 的关系（需新建，可由 HTML 渲染）

## 过程（主体）

> Blog 删掉的混乱，视频要留下。保留卡住、报错、试错和当时的判断。

### 段落 1 - 批准只花了一句话

**旁白：**

生成出来的 skill 有 frontmatter，有分步流程，有硬约束清单，还有验收标准。该有的都有，读起来像个成熟的东西。

我批准它只用了一句话：可以，写出来。

**画面：** 记录卡 —— `25865db6-d076-47e5-84d7-950aed05ca24.jsonl` @ `2026-08-09T07:11:50`
> 可以，写出来，后续软链我想办法建

**证据级别：** 一手（原始会话记录，非重演）
**要留的瞬间：** 批准那一句的轻率，与产物外观的完整形成对照

### 段落 2 - 配置在骂模板

**旁白：**

跑出来的第一篇文案枯燥得读不下去。翻开模板才看见根子：里面硬编码了三段列表——结果、有效的、踩的坑。

而在同一套配置的另一个文件里，写着一句批评：陈述完事实就转进列表，没有处境，没有反应。

配置在骂模板，模板在违反配置。两个文件分开看，每一个都是对的。

**画面：** 两个文件并排对照（可复用 `drafts/xiaohongshu/assets/02-contradiction.png` 改横版）
**要留的瞬间：** 两份文本并排出现的那一刻

### 段落 3 - 一条从没能执行的规则

**旁白：**

再往下翻，配图那一节写着：输出 SVG 源文件，由人渲染。

我查了这台机器。rsvg-convert、ImageMagick、wkhtmltoimage，一个都没装。

这条规则从写下的那天起，就没有执行的可能。

**画面：** ❌ 需要终端 `command -v` 逐个返回 NOT FOUND 的录屏 —— **无素材，可复现补录**
**要留的瞬间：** 四个 NOT FOUND 连续刷出来

## 数据

**旁白：**

这次的数字：12 个 skill 文件，5 个平台配置，平台工具依赖 0。校验脚本写完第一次跑，就抓出 4 处死链——之前那是人工 review 才发现的。

耗时和 token 没有记录，录屏 0 个。这两项是这次最大的遗憾。

**画面：** 数据卡（可由 HTML 渲染，尺寸 1920×1080）

## 能不能复制

**旁白：**

这套东西换个人能不能用？

这是这次采用的流程策略：把确定性的部分沉进脚本，把「不要什么」逐条写进指令。换模型、题目或渠道后是否仍然有效，需要另行验证。

不可复制的是这次的具体产物。平台配置是照着这几个渠道写的，换个渠道得重写。

**画面：** 三层职责图（可复用 `drafts/xiaohongshu/assets/04-layers.png` 改横版）

## 结论

**旁白：**

{{作者手写。README Conclusion 目前是草稿，未定稿。}}

<!-- TODO: conclusion must be written by the author -->

**画面：** {{待定}}

## 制作备注

- 本文件已过时，见顶部状态表。制作请用 `scenes.md`。
- 中文字幕必做，多数人静音看。
- 终端画面字号要放大，手机端可读。
- 先定稿旁白，再对齐画面。
- 不要用转场和音乐掩盖信息不足。
- 「批准那一句」有原始记录，不需要复现：`25865db6.jsonl` @ `2026-08-09T07:11:50`。当时写「无法复现」是没想到去查会话记录。
- 「四个 NOT FOUND」可复现补录，必须标注为复现。
