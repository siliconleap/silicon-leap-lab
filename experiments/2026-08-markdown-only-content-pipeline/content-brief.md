# 内容简报：半天搭好流水线，第一篇却卡了一周多

## 背景与问题

我读了很多研究报告和前沿资讯，也常常觉得某个方向值得做，但没有真正把尝试落到手上。回头看，这是一种眼高手低：输入不断增加，行动没有形成闭环；想做的事越来越多，却没有沉下心走完「开始尝试、留下记录、整理内容、公开复盘」这些基础步骤。

这不只发生在 AI 领域。很多人想学一项技能、做一个项目或改变一种习惯时，也会卡在同一处：知道得越来越多，第一步却迟迟没有开始。AI 时代，这些基础步骤理应不再是主要拦路虎；它们可以被更低成本地协助完成，但仍需要人决定从哪件事开始，并把过程走完。

于是我把每个想法当作实验：一个目录、一份记录、一次公开。第一个实验是用 AI 搭建这套记录到多平台初稿的流水线。

## 读者痛点 / 叙事线

**作者判断：** 这次不只是在搭一套 AI 内容工具，更是在处理一种常见的停滞：知道得越来越多，却没有把最基础的尝试走完。AI 可以降低开始、记录和整理的门槛，但不能替人完成判断、承担公开的代价，或迈出第一步。

## 关键词与用法

| 关键词 | 类型 | 用法 |
| --- | --- | --- |
| AI agent | 事实对象 | 在背景或 setup 中说明流水线由 agent 协助搭建与运行。 |
| agent harness | 作者类比 | 第一次出现时解释为「把输入、约束、工具、人工确认和反馈串起来的运行环境」；本实验的内容流程属于这一类。 |
| human-in-the-loop | 事实对象 | 指作者确认内容简报、判断结论、审核与发布的必要环节。 |
| AI 原生工作流 | 读者讨论的叫法 | 只用于讨论 AI 降低开始、记录和整理门槛，不暗示发布可无人完成。 |
| 内容流水线 | 事实对象 | 指实验目录、skill、内容简报、审核和平台稿的这一条链路。 |

## 认知（作者判断，2026-08-22 补）

1. **AI 确实能一次为多个平台生成不同风格的文案，这件事也确实能自动化**——但没有网上说的那么顺。自动化本身要打磨，跟 AI 许一个愿换不来一篇能发的文章。
2. **优质文本不该靠抽卡。** 同一个模型，给什么约束就出什么水平。真正起作用的是工程约束：写清不要什么的 skill、逐条落地的 prompt、简报确认与独立审核这套流程。换更大的模型只会得到一个更精致的均值。
3. **能自动化的和不能自动化的，界限比预想的清楚。** 纯文本链路（选题 → 记录 → 多平台稿）走得通；一到需要真实画面的视频，纯文本这套办法就断了。

## 本次发生了什么

流水线骨架半天搭完：`experiment-plan` 负责实验前剧本，`content-forge` 负责把跑完的实验变成内容初稿。实际运行后，第一篇小红书读起来平淡。追查发现，平台配置批评「陈述完事实就转进列表」，模板却硬编码了「结果 / 有效的 / 踩的坑」三段列表；配图规则还依赖机器上不存在的渲染器。校验脚本首次运行抓到 4 处死链。

## 过程与困难清单（按发生顺序）

| # | 卡在哪 | 试了什么 | 代价 | 怎么收场 |
| --- | --- | --- | --- | --- |
| 1 | 不知道下一步做什么，仓库骨架有了但没内容 | 读 forge 的约定文档，才看清流水线只有中段 | 半天 | 决定先补前段，写 `experiment-plan` |
| 2 | skill 放哪：软链还是实拷 | 先建相对软链，轮到 lab 发现已有两个绝对路径软链 | 一次返工 | 改实拷；`cp -R` 目标已存在，踩出 `skills/skills/` 嵌套 |
| 3 | 初始 commit 的 author 是个人身份，不是品牌身份 | 查 git 历史发现；仓库从未 push | 一次历史重写 | `--reset-author` 修好，并用 `includeIf` 按目录自动切身份，3/3 场景实测通过 |
| 4 | 生成的 skill 看起来完整，跑出来的小红书文案读不下去 | 翻模板才发现硬编码三段列表，而同一套配置的另一个文件正在批评这种写法 | 全流程返工 | 模板改回叙事结构，把「不要什么」逐条写进指令 |
| 5 | 配图规则要求「输出 SVG 由人渲染」，本机没有任何渲染器 | 查 rsvg-convert / ImageMagick / wkhtmltoimage，一个都没装 | 规则作废重写 | 改成 HTML → 系统 Chrome headless → PNG，零额外安装 |
| 6 | 初稿不满意，反复改流程和文案，越改越不肯发 | 加规则、改模板、换写法，来回多轮 | 一周多（精确工时未记录） | 把打磨放进流程：简报确认 + 独立审核 + 三轮上限 |
| 7 | 文案生硬，像 AI 写的 | 装 Waza 的 `write` skill，接成渲染后的独立润色步 | 5 个文件 26 行改动 | 有效；但内容层返工后必须重跑，否则新段落照样生硬 |
| 8 | 视频交付不了 | 分镜纯文本 + TTS 配音 + 画面按配音时长铺开，跑出一版 4 分 24 秒 | 一整条链路 | 判为失败：录屏为 0，画面全是记录卡和结构图，没有一帧真实现场 |

## 实践清单

| 实践 | 是否有效 | 是否可复制 |
| --- | --- | --- |
| 让 AI 搭一条自动化流水线，一次产出多平台文案 | 有效 | 可复制 |
| 参考社区现成 skill（Waza 的 `write`）提升文案质量 | 有效 | 可复制 |
| 用流程约束替代反复抽卡：简报确认 → 独立审核 → 三轮上限 | 有效 | 可复制 |
| 把确定性动作沉进脚本（目录骨架、校验、配图渲染） | 有效 | 可复制 |
| 让 AI 反推图片、合成视频 | 目前无效 | 不可复制 |

## 作者判断

上表第 6 行是这次真正的代价：拖住的不是代码，是我不满意初稿又不肯放手。

这次只能说明：该生成过程没有自行发现跨文件矛盾，也不能替我判断文章是否值得发；它不能证明所有模型都会如此，更不能推出换模型没有用。我的判断是：初稿必须进入可追溯的审核，不能直接当成成品。

## 超出骨架预期的发现与边界

「平台工具依赖为 0」不等于「发布成本为 0」。注册和维护账号、补真实截图、确认个人观点、手机预览、排版发布和后续维护仍由人承担。视频素材为 0，当前只能生成脚本结构，不能称为可发布视频。

下一版流程应为：实验前确认背景与叙事骨架；实验后先写这类短简报；作者确认事实、观点和公开范围后，再展开为不同平台的稿件，并最多经历 3 轮独立审核和修改。

## 作者确认

已确认：是；日期：2026-08-15；可公开：是。
